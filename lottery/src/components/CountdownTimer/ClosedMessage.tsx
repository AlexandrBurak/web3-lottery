import React, { FC } from "react";

const ClosedMessage: FC = () => {
  return (
    <div>
      <h2 className="text-white text-xl text-center animate-bounce">
        Ticket Sales have now CLOSED for this draw
      </h2>
    </div>
  );
};

export default ClosedMessage;
